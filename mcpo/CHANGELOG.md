# Changelog

## 1.0.0

- Initial release
- MCPO proxy with configurable MCP servers via JSON
- Support for stdio, SSE, and streamable-http server types
- Optional API key authentication
- Exposed port 8000 for remote Open WebUI connections
